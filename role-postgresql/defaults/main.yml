---
# defaults file for role-postgresql
replica_password: 'replica123'
db_password: 'postgres123'
postgresql_rpms:
  - "postgresql12-12.5-1PGDG.rhel7.x86_64.rpm"
  - "postgresql12-libs-12.5-1PGDG.rhel7.x86_64.rpm"
  - "postgresql12-server-12.5-1PGDG.rhel7.x86_64.rpm"
# Public network is the one that the outside worlds connects over
# Replication network is used for replication between the nodes
public_network: "192.168.98"
replication_network: "192.168.99"